[pytest]
asyncio_mode = auto
testpaths = backend/tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    docker: mark test as requiring a running Docker daemon (deselect with '-m "not docker"')
